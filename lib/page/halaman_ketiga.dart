import 'package:flutter/material.dart';

class HalamanKetiga extends StatelessWidget {
  const HalamanKetiga({super.key, required this.alamat, required this.nama, required this.notelp, required this.makanan, required this.minuman});

  final String nama;
  final String notelp;
  final String alamat;
  final String makanan;
  final String minuman;
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}