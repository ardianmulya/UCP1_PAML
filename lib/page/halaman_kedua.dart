import 'package:flutter/material.dart';

class HalamanKedua extends StatelessWidget {
  const HalamanKedua({super.key, required this.nama, required this.notelp, required this.alamat});

  final String nama;
  final String notelp;
  final String alamat;
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}