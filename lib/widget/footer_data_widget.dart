import 'package:flutter/material.dart';

class FooterDataWidget extends StatelessWidget {
  const FooterDataWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}